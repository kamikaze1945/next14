query ProductsGetByCategorySlug($slug: String!) {
	category(slug: $slug) {
		products {
			id
			name
			description
			price
			images {
				url
			}
			categories {
				slug
				name
			}
			collections {
				name
				slug
			}
			meta {
				count
				total
			}
		}
	}
}
