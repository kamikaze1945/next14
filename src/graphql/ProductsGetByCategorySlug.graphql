query ProductsGetByCategorySlug($slug: String!) {
	categories(where: { slug: $slug }) {
		products(first: 10) {
			id
			name
			description
			categories {
				name
			}
			images {
				url
			}
			price
		}
	}
}
